version: "1.0"
defaults:
  base_dir: ".worktrees"

hooks:
  post_create:
    - type: copy
      from: ".env"
      to: ".env"

    - type: copy
      from: "workflows/"
      to: "workflows/"

    - type: command
      command: mise trust
